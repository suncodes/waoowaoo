生成一张独立的修复候选图片。系统可能多次调用本提示词生成多个候选；每个候选都必须独立遵守目标规格。保持 preserve 中已正确的内容，只修复 PromptPatch 指出的失败维度。

原始提示词：
{base_prompt}

目标规格：
{target_spec_json}

PromptPatch：
{prompt_patch_json}

要求：
- 不要添加未要求的新主体、文字、道具、场景或风格；遵守 add；避免 remove 和 negative。
- 构图、角色身份、道具身份、场景和光色中已正确的部分必须稳定延续。
- 一张图只有一个时空和一个主要视觉事件，禁止拼贴、多格、素材墙和连续动作分镜。
- 风格只控制视觉语言，禁止引入风格关联作品中的角色、脸、身体、建筑、徽标或标志物。
- 若问题为主体或连续性错误，重新建立正确主体，不继承错误角色；若为局部编辑，不得重绘无关正确区域。
- 图像中禁止文字、水印和标志；准确文案由后续合成完成。
